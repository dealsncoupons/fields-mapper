Building a jenkins pipeline

1. View process running on a port
netstat -ano | findStr 8080

2. Kill process running on a port
taskkill /F /PID 16180

1. Run jenkins docker image
docker run --rm \
-p 8080:8080 \
-p 50000:50000 \
-v jenkins_home:/var/jenkins_home \
jenkins/jenkins:lts
