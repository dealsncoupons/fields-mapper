pipeline {
    agent any

    stages {
        stage('clean') {
            step {
                echo 'cleaning step'
            }
        }

        stage('build') {
            step {
                echo 'building step'
            }
        }

        stage('deploy') {
            step {
                echo 'deploying step'
            }
        }
    }
}